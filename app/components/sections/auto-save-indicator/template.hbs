<div class="flex-col">
  {{yield}}

  <div
    class="pt-6 h-8 flex justify-end font-semibold text-blue"
  >
    {{#if autoSaveIndicator.isSaving}}
      <span class="text-orange">SAVING ...</span>
    {{else}}
      AUTO SAVED
      <span class="ml-4">{{fa-icon "check"}}</span>
    {{/if}}
  </div>
</div>