<div class="btn-blue {{if loading "cursor-wait"}}">
  <span class="text-3xl mr-2 uppercase">{{label}}</span>
  <div class="w-10 flex justify-center text-3xl">
    {{#if loading}}
      {{fa-icon "cog" spin=true}}
    {{else}}
      {{fa-icon icon}}
    {{/if}}
  </div>
</div>
