<div class="relative min-h-screen">
  {{navs/main-nav}}
  <div class="ml-24">
    {{outlet}}
  </div>

    {{!-- {{#burger-menu
      class="min-h-screen w-full flex flex-1 flex-col items-left"
    as |burger|
    }}
      {{#burger.menu itemTagName="li" dismissOnItemClick=true as |menu|}}
        <div class="flex justify-end">
          <button {{action burger.state.actions.close}}>
            {{fa-icon "close" class="p-4 text-5xl"}}
          </button>
        </div>

        <ul class="m-0 p-0">
          {{#menu.item}}
            {{link-to
              "Productions"
              "authenticated.productions"
            }}
          {{/menu.item}}

          {{#menu.item}}
            {{link-to
              "Products"
              "authenticated.products"
              data-test-menu-item-products
            }}
          {{/menu.item}}

          {{#menu.item}}
            {{link-to
              "Recipes"
              "authenticated.recipes"
              data-test-menu-item-recipes
            }}
          {{/menu.item}}

          {{#menu.item}}
            {{link-to
              "Ingredients"
              "authenticated.ingredients"
              data-test-menu-item-ingredients
            }}
          {{/menu.item}}

          {{#menu.item}}
            {{link-to
              "Settings"
              "authenticated.settings"
            }}
          {{/menu.item}}
        </ul>
      {{/burger.menu}}
      <button
        data-test-menu-toggle-button
        class="pin-t pin-l absolute ml-4 mt-4"
        {{action burger.state.actions.toggle}}
      >
        {{fa-icon "bars" class="text-5xl cursor-pointer"}}
      </button>

      <div class="min-h-full w-full self-center mt-20">
        {{#burger.outlet}}

          <div
            class="absolute pin-t pin-r p-4 flex items-baseline"
          >
            <p class="bold">{{session.session.authenticated.profile.email}}</p>
            <p class="ml-2 mr-2">|</p>

            {{ui/label-button
              class="rounded shadow bg-blue text-white p-2 pr-4 pl-4 hover:bg-blue-light"
              label="Logout"
              onClick=(action "logout")
            }}
          </div>

          <div class="mb-24">
            {{outlet}}
          </div>

        {{/burger.outlet}}
      </div>
    {{/burger-menu}} --}}
</div>
{{intercom-io}}
