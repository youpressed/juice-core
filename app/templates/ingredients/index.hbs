{{#containers/section-container}}
  {{lists/label-list
      label='Ingredients'
      rowType='rows/label-row'
      createAction=(route-action 'createNode')
      createActionLabel='ingredient'
      selectAction=(route-action 'showNode')
      destroyAction=(route-action 'destroyNode')
      model=model}}
{{/containers/section-container}}
